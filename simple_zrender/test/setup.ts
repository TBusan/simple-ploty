// Jest 测试设置文件

// 扩展 Jest 匹配器
expect.extend({
  // 自定义匹配器可以在这里添加
});

// 全局测试配置
beforeAll(() => {
  // 所有测试之前的设置
});

afterAll(() => {
  // 所有测试之后的清理
});

beforeEach(() => {
  // 每个测试之前的设置
});

afterEach(() => {
  // 每个测试之后的清理
});
